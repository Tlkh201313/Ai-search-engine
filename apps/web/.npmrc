# Copy files into node_modules instead of hard-linking the pnpm store.
# Something on this machine (AV real-time protection) deletes store-linked
# files (next/dist/compiled/jest-worker/processChild.js), breaking builds.
package-import-method=copy
