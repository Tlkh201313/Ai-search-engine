# Base URL of the FastAPI backend (no trailing slash).
NEXT_PUBLIC_API_URL=http://localhost:8000

# Optional: server-side rewrite target for /api-proxy/* (usually unnecessary).
# API_INTERNAL_URL=http://localhost:8000
