# --- Core API ---
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0

# --- HTTP + scraping ---
httpx>=0.27
beautifulsoup4>=4.12
lxml>=5.2
selectolax>=0.3.21
# Verify TLS via the OS trust store so hosts with incomplete cert chains (that
# work in browsers but fail OpenSSL/certifi) are reachable. See app/__init__.py.
truststore>=0.9

# --- Storage ---
aiosqlite>=0.20

# --- Optional, high-quality extraction (graceful fallback if missing) ---
trafilatura>=1.10

# --- Testing ---
pytest>=8.2
pytest-asyncio>=0.23
respx>=0.21

# --- Dev tooling ---
ruff>=0.6
